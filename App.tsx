import { SafeAreaView } from "react-native-safe-area-context";
import PostsFeed from "./src/screens/PostsFeed";

export default function App() {
  return (
    <SafeAreaView>
      <PostsFeed />
    </SafeAreaView>
  );
}
